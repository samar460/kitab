<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="main font-serif w-full min-h-screen bg-[#D9AFD9] bg-gradient-to-t from-[#D9AFD9] to-[#97D9E1] p-8 text-black-300"
    >
      <div class="w-full flex justify-between mb-4">
        <h2 class="text-xl text-center text-red-600">KITAB 📝</h2>
        <a
          href="/logout"
          class="bg-red-700 rounded-md px-3 py-2 text-sm text-white mb-1 inline-block"
          >Logout</a
        >
      </div>
      <div class="flex items-start gap-3">
        <div class="w-13 h-13 flex flex-col items-center overflow-hidden">
          <div class="w-12 h-12 rounded-md overflow-hidden">
            <img
              src="/profile/image/<%= user._id %>"
              alt="Profile Image"
              class="w-full h-full object-cover rounded-md"
            />
          </div>
          <a href="/uploaddp" class="text-xs text-zinc-600 mt-2">Edit</a>
        </div>
        <h2 class="text-3xl mb-3">
          <span class="font-light">Hello</span>, <%=user.name%> 👋
        </h2>
      </div>
      <h5 class="mb-2 mt-4">You can create a new post.</h5>
      <form action="/post" method="post">
        <textarea
          class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 rounded-md border-zinc-800"
          placeholder="What's on your mind ?"
          name="content"
        ></textarea>
        <input
          type="submit"
          value="Create New Post"
          class="px-3 py-2 text-sm text-white w-30 bg-blue-500 block rounded-md mt-2"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-900 text-xl font-bold">Posts:</h3>
        <div
          class="postcontainer mt-5 flex gap-5 flex-wrap justify-center w-full"
        >
          <% allposts.reverse().forEach(function(post){ %>
          <div
            class="post w-full sm:w-1/2 md:w-1/4 p-4 flex flex-col justify-between mb-4 rounded-md border-[1px] border-zinc-700 bg-gradient-to-t from-[#D9AFD9] to-[#97D9E1]"
          >
            <h4 class="text-red-900 text-sm mb-1">
              @<%= post.user.username %>
            </h4>
            <p class="tracking-tight mb-3"><%= post.content %></p>
            <small class="mt-2 block"><%= post.likes.length %>❤️</small>
            <div class="btns flex justify-between">
              <a href="/like/<%= post._id %>" class="text-red-500">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a>
              <a href="/delete/<%= post._id %>" class="text-zinc-600">Delete</a>
              <a href="/edit/<%= post._id %>" class="text-zinc-600">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
