<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="main font-serif w-full min-h-screen bg-[#D9AFD9] bg-gradient-to-t from-[#D9AFD9] to-[#97D9E1] p-8 text-black"
    >
      <div class="w-full flex justify-between mb-4">
        <h2 class="text-xl text-center text-red-600">KITAB 📝</h2>
        <a
          href="/logout"
          class="bg-red-700 rounded-md px-3 py-2 text-sm text-white mb-1 inline-block"
          >Logout</a
        >
      </div>
      <h5 class="mb-1">Edit Your Post:</h5>
      <form action="/update/<%= post._id%>" method="post">
        <textarea
          class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 rounded-md border-zinc-800"
          placeholder="<%=post.content%>"
          name="content"
        ></textarea>
        <input
          type="submit"
          value="Update Post"
          class="px-3 py-2 text-sm text-white w-30 bg-blue-500 block rounded-md mt-2"
        />
      </form>
    </div>
  </body>
</html>
